image: acastellano/debbuilder

stages:
    - build

dev_package:
    stage: build
    script:
        - dpkg-buildpackage -uc -b
        - mkdir build
        - mv ../*.deb build/
    artifacts:
        paths:
            - build/*
