image: acastellano/debbuilder

stages:
    - build

dev_package:
    stage: build
    script:
        - dpkg-buildpackage -uc -b
        - mkdir build
        - mv ../*.deb build/
        - PACKAGE=$(ls build)
        - echo $PACKAGE
    artifacts:
        paths:
            - build/*
