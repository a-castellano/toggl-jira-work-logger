image: acastellano/debbuilder

stages:
    - build

dev_package:
    stage: build
    script:
        - mkdir -p builds/a-castellano/toggl-jira-work-logger/debian/toggl-jira-work-logger/usr/bin
        - dpkg-buildpackage -uc -b
